<div class="pure-u-1">
  <% @posts.each_with_index do |post, index| %>
    <article>
      <a href="post/<%= index %>">
        <h1><%= post.title %></h1>
        <h6>By <%= post.author %> - <%= post.date %></h6>
      </a>
    </article>
  <% end %>
</div>